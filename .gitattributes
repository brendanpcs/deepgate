# Normalise to LF in the repository; check out native on Windows.
* text=auto

# The Gradle wrapper script is executed by sh and must keep LF even on Windows.
gradlew text eol=lf
*.sh text eol=lf

# Windows scripts keep CRLF.
*.bat text eol=crlf

# Never touch binaries.
*.jar binary
*.png binary
